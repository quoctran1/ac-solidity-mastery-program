{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "hardhat": "hardhat node",
    "size": "hardhat size-contracts",
    "clean": "hardhat clean",
    "run:localhost": "hardhat run --network localhost",
    "deploy:local": "hardhat ignition deploy --network localhost",
    "deploy:sepo": "hardhat ignition deploy --network sepoliatest",
    "deploy:metis": "hardhat ignition deploy --network metistest"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "hardhat-abi-exporter": "^2.10.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
